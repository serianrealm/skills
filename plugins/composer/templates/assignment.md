# ASSIGNMENT

## Task Identity
- Feature: `<feature_name>`
- Task Slug: `<task_slug>`
- Worker Type: `architecturer|developer`
- Branch: `<task_slug>/<feature_name>`
- Worktree: `<absolute_path>`

## Current Command
- Attempt: 1

`<single current command for this worker>`

## Objective Goal

`<objective this worker must achieve>`

## Implementation Guidelines

`<interfaces, architecture, style constraints, and exact boundaries>`

## Context Packet

`<project structure, important files, APIs, and prior decisions the worker needs>`

## Acceptance Criteria

`<verifiable criteria composer will check>`

## Validation Commands

`<commands this worker may run>`

## Forbidden Actions

`<role-specific prohibitions>`

## Handoff Requirements

Worker must write `.agents/tasks/<task_slug>/HANDOFF.md` before stopping and include changed files, validation output, risks, and incomplete work.

## Revision Log

### Attempt 1

`<initial command>`
